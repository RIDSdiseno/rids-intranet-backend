node_modules
# Keep environment variables out of version control
.env

/generated/prisma
NUL
.DS_Store
src/certs/*.pfx

# Respaldos de datos generados por scripts de migración (contienen datos reales)
scripts/*backup*.json
